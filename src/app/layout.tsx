import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Anjaan Musafir | Your Personal Travel Companion",
  description: "Anjaan Musafir is your personal travel companion, providing personalized travel recommendations and itineraries based on your preferences and interests. Whether you're a solo traveler, a couple, or a family, Anjaan Musafir helps you discover new destinations and plan unforgettable trips.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
